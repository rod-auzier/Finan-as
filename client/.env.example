# Variáveis do frontend (opcional). Prefixo VITE_ é obrigatório para
# que o Vite exponha a variável ao código do navegador.

# URL base da API. Em desenvolvimento pode ficar vazio: o Vite faz proxy
# de /api para o backend (ver vite.config.js).
VITE_API_URL=/api
