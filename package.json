{
  "name": "painel-financeiro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Painel financeiro pessoal — deploy na Vercel (frontend estático + API serverless em /api)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.0"
  }
}
